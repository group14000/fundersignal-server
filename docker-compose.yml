services:
  postgres:
    # PostgreSQL latest major (pg18) with pgvector extension preinstalled
    image: pgvector/pgvector:pg18-trixie
    container_name: fundersignal-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./docker/init/01-pgvector.sql:/docker-entrypoint-initdb.d/01-pgvector.sql:ro

  redis:
    image: redis:7-alpine
    container_name: fundersignal-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6380}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
  redis_data:
